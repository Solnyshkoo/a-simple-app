# Just a simple app

The app uses your location and displays it in a textView. 
In settings you can switch it of also there are three slidres which set up a background colour.
Settings apear in three diferrent forms(depends on the tap amount). And all of them connected to each other.
